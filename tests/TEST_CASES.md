## How to Run the Tests
1. Save the provided JSON payloads into their respective isolated folders (`tests/fixtures/vulnerability/` or `tests/fixtures/audit/`).
2. Execute the test suite: `pytest run_tests.py`.
3. Check the `tests/output/agentic_scans/` directory to verify the expected outcomes.
